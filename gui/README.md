# Prism GUI

Electron desktop shell for Prism. Currently a static visual prototype —
sidebar nav is fully clickable, but only layout/styling is real; no view
calls the actual `prism` CLI yet.

## Run

```bash
npm install
npm start
```
